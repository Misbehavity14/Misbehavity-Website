export function HeroSection() {
  return (
    <section id="hero" className="section-shell">
      <div className="divider-line mb-10" />
      <p className="section-kicker">
        Hero Section
      </p>
      <h1 className="mt-6 max-w-5xl text-5xl font-semibold leading-tight text-foreground sm:text-7xl">
        Misbehavity landing page <span className="yellow-mark">hero</span>{" "}
        placeholder.
      </h1>
      <p className="section-copy">
        This area will later become the primary first impression for the
        one-page website.
      </p>
    </section>
  );
}
